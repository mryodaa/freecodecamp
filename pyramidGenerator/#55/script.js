const character = "#";
const count = 8;
const rows = [];

function padRow(name) {
    return name;
}

function addTwoNumbers(a, b) {
    return a + b;
}
const sum = addTwoNumbers(5, 10)
console.log(sum)